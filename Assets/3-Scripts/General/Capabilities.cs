using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Capabilities : MonoBehaviour
{
    // a list of variables that indicate what the game object they are attached to can and cannot do
    public bool canUseSwitches;
}
